<p id="notice"><%= notice %></p>

<p>
  <h2><%= @style.style %></h2>
</p>

<p>
  <%= @style.description %>
</p>
<p>
	<h4> <%= "beers" %> </h4>
</p>
<p>
  <% @style.beers.each do |beer| %>
      <li> <%= link_to beer.name, beer %> 
      	<%= "by" %>
      	<%= link_to beer.brewery.name, beer.brewery %>
      </li>
  <% end %>
</p>
<%= link_to 'Edit', edit_style_path(@style) %> |
<%= link_to 'Back', styles_path %>
